[build-system]
requires = ["maturin>=1.0,<2.0"]
build-backend = "maturin"

[project]
name = "chronomind"
version = "0.2.0"
description = "Python bindings for the ChronoMind lock-free HNSW index (benchmark harness)"
requires-python = ">=3.9"

[tool.maturin]
features = ["pyo3/extension-module"]
